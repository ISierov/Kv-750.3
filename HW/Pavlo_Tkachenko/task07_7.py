"""
We need a function that can transform a number (integer) into a string.
"""
def numToStr(num_int):
    s = str(num_int)

    return s


print(numToStr(-345))
print(numToStr(1+2))
print(numToStr(0))

